<!-- filepath: c:\Users\24016580\Downloads\Year 2 Sem 2\C372 PayTech\lesson 3 deliverables\SupermarketMVC\SupermarketApp\views\checkout.ejs -->
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Checkout Complete</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Supermarket App</a>
    </div>
  </nav>

  <div class="container mt-4">
    <h1>Checkout Complete</h1>
    <p>Thank you, <%= user ? user.username : 'guest' %>. Your order was processed.</p>

    <h2>Items</h2>
    <ul>
      <% if (items && items.length) { %>
        <% items.forEach(function(item){ %>
          <li><%= item.productName %> — qty: <%= item.quantity %> — price: $<%= Number(item.price).toFixed(2) %></li>
        <% }) %>
      <% } %>
    </ul>

    <p><strong>Total: $<%= Number(total).toFixed(2) %></strong></p>

    <p><a href="/shopping" class="btn btn-primary">Continue shopping</a></p>
  </div>
</body>
</html>