<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Update Product</title>
  <style>
    body {
      background: radial-gradient(circle at 10% 20%, #f3ffe2 0, #eef6ff 40%, #f7f7ff 75%);
      min-height: 100vh;
      color: #1f2937;
    }
    .navbar-brand {
      font-weight: 700;
      letter-spacing: 0.5px;
    }
    .page-hero {
      text-align: center;
      margin: 30px 0 10px;
    }
    .page-hero h2 {
      font-weight: 800;
      letter-spacing: 0.4px;
      color: #0f172a;
    }
    .page-hero p {
      color: #475569;
      margin: 6px 0 0;
    }
    .editor-card {
      background: #ffffff;
      border: 1px solid #e5e7eb;
      box-shadow: 0 18px 40px rgba(15, 23, 42, 0.08);
      border-radius: 16px;
      padding: 28px;
    }
    .form-label {
      font-weight: 600;
      color: #0f172a;
    }
    .input-group-text {
      background: #f8fafc;
      border-right: none;
      color: #64748b;
      font-weight: 600;
    }
    .form-control:focus {
      box-shadow: 0 0 0 0.15rem rgba(37, 99, 235, 0.25);
      border-color: #2563eb;
    }
    .btn-primary {
      background: linear-gradient(135deg, #2563eb, #7c3aed);
      border: none;
      font-weight: 700;
      box-shadow: 0 10px 25px rgba(37, 99, 235, 0.25);
    }
    .btn-outline-secondary {
      border-width: 2px;
      font-weight: 700;
    }
    .preview-card {
      background: #0f172a;
      color: #e2e8f0;
      border-radius: 14px;
      padding: 18px;
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.05);
      min-height: 100%;
    }
    .preview-chip {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 6px 10px;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.1);
      color: #cbd5e1;
      font-size: 12px;
      letter-spacing: 0.3px;
    }
    .thumb {
      border-radius: 12px;
      background: #111827;
      padding: 12px;
      border: 1px solid rgba(255,255,255,0.08);
      display: inline-block;
    }
    .small-label {
      text-transform: uppercase;
      letter-spacing: 0.1em;
      font-size: 11px;
      color: #94a3b8;
      margin-bottom: 6px;
      display: block;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark shadow-sm">
    <div class="container">
      <a class="navbar-brand" href="/">Supermarket App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/inventory">Inventory</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/addProduct">Add New Product</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="page-hero">
      <h2>Update Product</h2>
      <p>Refresh details, pricing, and imagery in one place.</p>
    </div>

    <% if (product) { %>
      <% const displayName = product.productName || product.name || 'Product'; %>
      <% const displayImage = (product.image || '').startsWith('/images/') ? product.image : `/images/${product.image}`; %>
      <div class="row g-4 align-items-start">
        <div class="col-lg-7">
          <div class="editor-card">
            <form action="/updateProduct/<%= product.id %>" method="POST" enctype="multipart/form-data">
              <div class="row g-3">
                <div class="col-md-8">
                  <label for="name" class="form-label">Product Name</label>
                  <input type="text" class="form-control" id="name" name="name" value="<%= displayName %>" required>
                </div>
                <div class="col-md-4">
                  <label for="quantity" class="form-label">Quantity</label>
                  <input type="number" class="form-control" id="quantity" name="quantity" min="0" step="1" value="<%= product.quantity || product.stock %>" required>
                </div>

                <div class="col-md-4">
                  <label for="price" class="form-label">Price</label>
                  <div class="input-group">
                    <span class="input-group-text">$</span>
                    <input type="number" class="form-control" id="price" name="price" min="0" step="0.01" value="<%= product.price %>" required>
                  </div>
                </div>
                <div class="col-md-4">
                  <label for="discount" class="form-label">Discount (%)</label>
                  <div class="input-group">
                    <input type="number" class="form-control" id="discount" name="discount" min="0" step="0.01" value="<%= typeof product.discount !== 'undefined' ? product.discount : 0 %>">
                    <span class="input-group-text">%</span>
                  </div>
                </div>
                <div class="col-md-6">
                  <label for="category" class="form-label">Category</label>
                  <input type="text" class="form-control" id="category" name="category" value="<%= product.category || '' %>" placeholder="e.g. Beverages" required>
                </div>
                <div class="col-md-6">
                  <label for="description" class="form-label">Short Description</label>
                  <textarea class="form-control" id="description" name="description" rows="2" placeholder="Tell shoppers why this product stands out"><%= product.description || '' %></textarea>
                </div>

                <div class="col-md-6">
                  <label class="form-label">Current Image</label>
                  <input type="text" class="form-control" name="currentImage" value="<%= product.image %>" readonly>
                  <div class="mt-2">
                    <img src="<%= displayImage %>" class="img-fluid rounded shadow-sm" style="max-height: 160px;" alt="<%= displayName %>">
                  </div>
                </div>
                <div class="col-md-6">
                  <label for="image" class="form-label">Upload New Image</label>
                  <input class="form-control" type="file" id="image" name="image" accept="image/*">
                  <div class="form-text">Leave blank to keep the current image.</div>
                </div>
              </div>

              <div class="d-flex gap-2 mt-4">
                <button type="submit" class="btn btn-primary flex-grow-1">Save Changes</button>
                <a href="/inventory" class="btn btn-outline-secondary">Back</a>
              </div>
            </form>
          </div>
        </div>

        <div class="col-lg-5">
          <div class="preview-card">
            <div class="d-flex justify-content-between align-items-center mb-2">
              <span class="preview-chip">Live Preview</span>
              <span class="small-label">ID #<%= product.id %></span>
            </div>
            <h5 class="mb-2"><%= displayName %></h5>
            <div class="d-flex align-items-center gap-3 mb-3">
              <div class="thumb">
                <img src="<%= displayImage %>" alt="<%= displayName %>" style="width: 120px; height: 120px; object-fit: contain;">
              </div>
              <div>
                <div class="small-label">Pricing</div>
                <div class="fs-5 fw-bold text-white mb-1">$<%= Number(product.price || 0).toFixed(2) %></div>
                <div class="text-success small">Discount: <%= Number(product.discount || 0).toFixed(2) %>%</div>
                <% const liveDiscountPrice = Number(product.price || 0) * (1 - Number(product.discount || 0) / 100); %>
                <div class="text-info small">
                  Discount price:
                  <% if (Number(product.discount || 0) > 0) { %>
                    $<%= liveDiscountPrice.toFixed(2) %>
                  <% } else { %>
                    N/A
                  <% } %>
                </div>
              </div>
            </div>
            <div class="small-label">Category</div>
            <div class="mb-2 text-white"><%= product.category || 'Uncategorized' %></div>
            <div class="small-label">Description</div>
            <p class="mb-0 text-white-50"><%= product.description || 'No description yet.' %></p>
          </div>
        </div>
      </div>
    <% } else { %>
      <div class="text-center text-muted py-5">No product found.</div>
    <% } %>
  </div>
</body>
</html>
