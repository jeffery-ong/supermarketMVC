<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    :root {
      --bg: #f5f7fb;
      --panel: #ffffff;
      --border: #e5e7eb;
      --muted: #6b7280;
      --accent: #2563eb;
      --text: #0f172a;
    }
    body {
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
    }
    .card-glass {
      background: var(--panel);
      border: 1px solid var(--border);
      box-shadow: 0 12px 32px rgba(15, 23, 42, 0.08);
      color: var(--text);
      border-radius: 14px;
    }
    .pill {
      display: inline-flex;
      align-items: center;
      padding: 4px 10px;
      border-radius: 999px;
      font-weight: 600;
      font-size: 12px;
      color: var(--muted);
      background: #f1f5f9;
      border: 1px solid var(--border);
    }
  </style>
</head>
<body>
  <%- include('navbar') %>
  <main class="container py-4">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-3 mb-3">
      <div>
        <div class="text-muted">Revenue Overview</div>
        <h1 class="mb-0">$<%= totalRevenue %> collected</h1>
      </div>
      <div class="d-flex gap-2 flex-wrap">
        <a href="/admin" class="btn btn-outline-secondary btn-sm">Back to Dashboard</a>
        <a href="/purchase-history" class="btn btn-outline-secondary btn-sm">View Purchase History</a>
      </div>
    </div>

    <div class="card card-glass p-3 mb-3">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <h5 class="mb-0">Revenue by Date</h5>
        <span class="pill">Daily totals</span>
      </div>
      <% if (!series.length) { %>
        <div class="text-muted">No revenue data yet.</div>
      <% } else { %>
        <div class="table-responsive">
          <table class="table align-middle mb-0">
            <thead class="table-light">
              <tr>
                <th>Date</th>
                <th class="text-end">Revenue</th>
              </tr>
            </thead>
            <tbody>
              <% series.forEach(item => { %>
                <tr>
                  <td><%= item.date %></td>
                  <td class="text-end">$<%= item.total %></td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% } %>
    </div>
  </main>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
