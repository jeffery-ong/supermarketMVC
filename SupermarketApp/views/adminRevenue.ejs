<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #f8f9fa;
      color: #212529;
      min-height: 100vh;
    }
    .card-glass {
      background: #ffffff;
      border: 1px solid #e7eaef;
      box-shadow: 0 6px 18px rgba(0,0,0,0.06);
      color: #212529;
    }
  </style>
</head>
<body>
  <%- include('navbar') %>
  <main class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <div>
        <div class="text-muted">Revenue Overview</div>
        <h1 class="mb-0">$<%= totalRevenue %> collected</h1>
      </div>
      <div class="btn-group">
        <a href="/admin" class="btn btn-outline-light btn-sm">Back to Dashboard</a>
        <a href="/purchase-history" class="btn btn-outline-light btn-sm">View Purchase History</a>
      </div>
    </div>

    <div class="card card-glass p-3 mb-3">
      <h5 class="mb-3">Revenue by Date</h5>
      <% if (!series.length) { %>
        <div class="text-muted">No revenue data yet.</div>
      <% } else { %>
        <div class="table-responsive">
          <table class="table align-middle mb-0">
            <thead>
              <tr>
                <th>Date</th>
                <th class="text-end">Revenue</th>
              </tr>
            </thead>
            <tbody>
              <% series.forEach(item => { %>
                <tr>
                  <td><%= item.date %></td>
                  <td class="text-end">$<%= item.total %></td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% } %>
    </div>
  </main>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
